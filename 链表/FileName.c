#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#include"list.h"

/*×¢ÊÍ*/

int main(void)
{
	Node * head=list_create();



	/*Node* tail1 = NULL;
	Node* tail2 = NULL;
	
	add_back_tail(&tail1, 1);
	add_back_tail(&tail1, 3);
	add_back_tail(&tail1, 5);
	add_back_tail(&tail1, 8);
	add_back_tail(&tail1, 9);

	add_back_tail(&tail2, 0);
	add_back_tail(&tail2, 2);
	add_back_tail(&tail2, 4);

	display_list(tail1);
	display_list(tail2);
	display_list(mergeTwoLists(tail1, tail2));*/
	return 0;
}